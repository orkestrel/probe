# probe readiness matrix — reconciled 2026-08-20

Lanes returned: canon (Cursor Grok), objective (Sol), subjective (Opus reviewer), plus PBAUDIT4
(Sol, the cross-engine audit of the Opus-written fix rounds). All rows ruled. PBFIX12 is the Sol
objective unit; PBFIX13 is the Opus shape-and-prose unit, serial after it.

| Row | Finding | Ruling | Carrier |
| --- | ------- | ------ | ------- |
| BR1 | Objective: `findRefusedPaths` blames containment for a schema-invalid source — `{ text: '' }` yields `refused: ['case.files.0.path']` while `CLAIM_SHAPE` already rejects the input (reproduced by structure: helpers.ts:244 substitutes `text: ''` and tests only the path) | Accepted; return no refused paths unless the claim shape admits the input, red-then-green | PBFIX12 |
| BR2 | Objective: `computeReceipt` tests stage presence only, so duplicate `type` checks mint a receipt while guides/probe.md:255 requires one check per stage | Accepted; require each declared stage exactly once in each phase, red-then-green | PBFIX12 |
| BR3 | Objective: `ProbeServer.start()` after `destroy()` silently returns (ProbeServer.ts:79) while the failure table promises `claimant`/`destroyed` | Accepted; keep the no-op only for an already-serving server, throw after teardown, red-then-green | PBFIX12 |
| BR4 | Objective: physical containment is check-then-use — a concurrent process can swap a checked directory for an outward symlink between the final check and `writeFileSync` (RuntimeStage.ts:390-394), falsifying the guide's unconditional containment sentence | Ruled: Node's portable fs API offers no descriptor-relative traversal, so the honest close is the documented boundary — the guarantee covers the inputs and the tree as inspected, never a concurrently mutating filesystem. The guide sentence and the write-path TSDoc state it | PBFIX12 |
| BR5 | PBAUDIT4: a native creation fault in `#specification` (a workspace file occupying the declared test directory fails `mkdirSync`) classifies to `instrument` through the catch ternary at RuntimeStage.ts:400-417, while the target tree owns the blocker | Accepted; classify target-tree creation faults as `workspace`, retain the D3 claimant exceptions, update the pinned test and taxonomy prose, red-then-green | PBFIX12 |
| BR6 | PBAUDIT4 (settles the subjective lane's referral R1): `LintStage` raises `progress` only when diagnostics arrive (:399) and `TypeStage` only in `#unblock` (:233), so an expiry after claimant work began but before publication is attributed to the instrument; executed probe evidence: `during === baseline` after `didOpen` | Accepted; raise `progress` when claimant-owned work is admitted, with a protocol-faithful held-diagnostics proof; the guide's `StageInterface` row then states the landed contract and names `RuntimeStage` as the stage that lowers before stage-owned work | PBFIX12 |
| BR7 | PBAUDIT4: the gauge proof's single FIFO path admits an interleaving where the cleanup reader attaches to the still-open claimant writer and samples elevated progress — a false red repeated greens cannot exclude | Accepted; distinct FIFO inodes per rendezvous, atomically replacing the cache path between phases, then drain the cleanup FIFO to end of file | PBFIX12 |
| BR8 | Orchestrator observation: with the workspace toolchain absent, the installed bin dies on an uncaught `ProbeError` stack over stderr before any protocol frame; guide Prerequisites documents the refusal-at-construction, not the presentation | Accepted; the bin entry reports the failure as one stderr line (origin, code, message) and exits 1, proof driven on the host | PBFIX12 |
| BR9 | PBAUDIT4 (falsifies the subjective lane's collision CONFIRMED): probe's `Source` collides with the `Source` class `@orkestrel/guide` exports (node_modules/@orkestrel/guide/dist/src/core/index.d.ts:962, verified by grep); the fleet standard set by the `Finding` precedent counts a declared development dependency | Accepted; rename probe's file-text record, with a fleet-wide collision sweep for the chosen name as the criterion | PBFIX13 |
| BR10 | Subjective 3a: `Verdict.checks` breaks one-concept-one-term — the member holds the claim's case outcomes, `formatVerdict` prints `case` for it (helpers.ts:93), and `Claim` names the phases `case`/`control` (verified) | Accepted; rename to `case`, sweeping declaration, validators, helpers, Probe, guide, and proofs | PBFIX13 |
| BR11 | Subjective 3b: `messageFromUnknown` follows no sanctioned helper form | Accepted; rename to `describeUnknown` with its consumers | PBFIX13 |
| BR12 | Canon 2 + subjective 2a/2b: banned senses — `now` at types.ts:358, :405 and helpers.ts:544; temporal `once` at guide:635; `below` at guide:717; `guarantees`+untethered `both` at server/types.ts:137; `below` comments at Probe.ts:232, :431, RuntimeStage.ts:712, TypeStage.ts:111 (the comparative `above` at RuntimeStage.ts:64 stays) | Accepted (hits verified by read); rule each by sense at edit time | PBFIX13 |
| BR13 | Subjective 5b: the guide never states what a successful `tools/call` returns — one rendered text block whose closing line carries the receipt, not the `Verdict` record | Accepted; add the reply-shape fact to § Registering the server in the section's own voice; the structured-content design decision is taken as a ROADMAP row rather than silence | PBFIX13 |
| BR14 | Subjective F4: § Registering names no driven client and states no honest limit; the only client ever driven is the repo's hand-written JSON-RPC writer | Accepted; where the installed `@orkestrel/mcp` client can drive a child stdio server, add that real-client proof; where it cannot, state the hand-written-client limit plainly and record the integration proof as a ROADMAP row | PBFIX13 |
| BR15 | Subjective F1 (ownership settled: scaffold's host inventory carries no `vite.bin.config.ts`, so the file is probe's): the comment names `scaffold` | Accepted; the comment names `probe` | PBFIX13 |
| BR16 | Subjective F2: `ProbeOptions.deadline` states no `Default:` line while the guide publishes 30,000 ms | Accepted | PBFIX13 |
| BR17 | Subjective F3: `PROBE_STAGES` derives from a hand-written union instead of the `as const` pattern its siblings use, so a `Stage` member absent from the list leaves `isStage` refusing silently; the package qualifier also diverges across the trio | Accepted; derive the union from the frozen list and settle the qualifier across the trio in one direction | PBFIX13 |
| BR18 | Subjective F6: example data drifts against itself — tool versions and the flagship digest disagree across `Toolchain`, `Verdict`, `isToolchain` examples and the guide's measured receipt | Accepted; align on the measured values with their date, or make the example visibly not the flagship claim | PBFIX13 |
| BR19 | Canon outside-claims: named function declarations inside an `it` callback at tests/src/server/helpers.test.ts:120, :123 (verified) | Accepted; hoist to module scope, keeping the name-vs-identity proof | PBFIX13 |
| BR20 | Canon 7 + objective 7 + subjective 7 (convergent): the artifact proof never drove the packed `dist/bin` surface | Partially closed by the Orchestrator, 2026-08-20: the installed entry exists with its `#!/usr/bin/env node` first line and symlinked `.bin/probe` (settling referral R2's shape half); the initialize round trip is blocked until the scratch consumer carries the workspace toolchain the guide's Prerequisites demand — the proof re-runs after the consumer rebuild, and its result rides in the acceptance record | Orchestrator proof v3 |
| BR21 | Objective claim 6: regression proofs missing for the seams BR1, BR2, BR3, and the containment boundary | Carried inside each row's red-then-green criterion | PBFIX12 |
| BR22 | Subjective lane's collision CONFIRMED on claim 3 | Falsified by BR9's executed evidence; recorded as overturned rather than dropped | recorded |
