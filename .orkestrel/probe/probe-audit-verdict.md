1. **BROKEN** — Containment is lexical, not physical. Executing `isSource` accepted `tmp/link/escape.test.ts` while rejecting `../escape.test.ts` and `/tmp/escape.test.ts`; `resolveWorkspaceFile('/workspace/root', 'tmp/link/escape.test.ts')` returned the lexical path without resolving symlinks. Therefore:

   - A test `Source.path` beneath an in-workspace symlink can make [`RuntimeStage`](/workspace/probe/src/server/stages/RuntimeStage.ts:329) create directories, write and run a specification, then unlink it outside the workspace.
   - A symlink swap between write and cleanup can redirect that unlink to a pre-existing file with the generated basename.
   - Candidate paths beneath symlinks let TypeScript and Oxlint inspect outside files.
   - A symlinked `Claim.project`, or an otherwise-contained project whose `extends`, `files`, `include`, or references escape, lets TypeScript read outside configuration and source.
   - A symlinked `tmp/probe` redirects fixed boot dependency writes, mutations, and deletion outside the workspace.

   Direct absolute and lexical `..` escapes are correctly refused. Generated files use `wx`, so an existing file at the original target is not overwritten. The guide states caller-code privileges and most writes, but not symlink reach, spawned-child environment inheritance, the Oxlint child’s exact process boundary, or these write/delete escapes. The symlink reproduction requires a writable fixture; the exact command after adding `tests/src/server/stages/RuntimeStage.symlink.test.ts` is `npx vitest run --config vite.config.ts --no-cache --reporter=verbose --project src:server tests/src/server/stages/RuntimeStage.symlink.test.ts`. The read-only sandbox prevented that probe. Smallest correct fix: enforce canonical containment for every existing path component and project read, reject symlink components before writes and cleanup, and document the remaining host privileges, spawned processes, inherited environment, and configuration reach.

2. **BROKEN** — [`Probe.destroy()`](/workspace/probe/src/server/Probe.ts:419) destroys its stages but never destroys its owned emitter. Executing the installed emitter showed `before 1 false` and, after `Emitter.destroy()`, `after 0 true`; searching `Probe.ts` found no `#emitter.destroy()` or `#emitter.clear()`. Existing Probe tests do not observe listener count or `emitter.destroyed` after teardown.

   Construction also lacks rollback. `Probe` starts `LintStage` before constructing `RuntimeStage`; if the workspace has readable manifests and a spawnable Oxlint binary but `vitest/node` fails to load, the constructor throws after the Oxlint child has spawned. No `Probe` value exists on which to call `destroy()`, so that child can keep the event loop alive. `ProbeServer` similarly constructs a Probe before later constructor operations that may throw. Existing tests cover ordinary stage teardown, signals, and server listeners, but not these constructor-failure states. Smallest correct fix: acquire stages through an initialization boundary that rolls back every earlier acquisition, destroy the emitter last, and add observable tests for listener count, emitter destruction, failed-constructor child exit, and transport-construction rollback.

3. **BROKEN** — The specification ownership marker is the terminal line `// @orkestrel/probe generated specification <pid>-<uuid>`, with the same revision in the filename. Executed helpers returned `false` for an unmarked file and `true` for a matching marker. However, [`RuntimeStage.#owned()`](/workspace/probe/src/server/stages/RuntimeStage.ts:527) treats the reserved boot paths as owned by path alone. A caller-owned, unmarked `tmp/probe/arm-type.probe-<dead-pid>-<uuid>.ts` or `arm-runtime...` is therefore deleted. The existing sweep test explicitly creates that unmarked collision and expects deletion, while an unmarked generated-name file elsewhere survives. The focused command is `npx vitest run --config vite.config.ts --no-cache --reporter=verbose --project src:server tests/src/server/stages/RuntimeStage.test.ts -t "removes the files a dead host left behind, at construction"`; it was not rerun because the brief forbids suite runs and the sandbox cannot create its fixture. Smallest correct fix: marker boot dependencies too and require matching path, revision, and marker for every sweep deletion.

4. **BROKEN** — An executed hand-built verdict containing clean case checks and both an `instrument` and a `code` finding at the declared control stage produced:

   `probe:case:type:typescript@6:oxlint@1:vitest@4:tsconfig.json@project`

   [`computeReceipt()`](/workspace/probe/src/core/helpers.ts:144) asks only whether the declared stage contains some `code` finding; it never rejects an `instrument` finding at that same stage. It correctly rejects findings at another control stage and case findings.

   A receipt currently binds the claim digest, declared stage, tool versions, and project path/digest. It does not authenticate the verdict, attest workspace cleanliness, attest test safety or side effects, cover `.oxlintrc.json`, `vite.config.ts`, or root `tsconfig.json`, require the control to be a mutation, require exactly one check per stage, or prove the declared stage had no instrument failure. The guide documents the intended limits, but its statement that the declared stage “counts `origin: 'code'` findings alone” encodes the defect and conflicts with its conclusion that an incomplete inspection cannot earn a receipt. Smallest correct fix: reject any instrument finding anywhere in either phase before minting the token, and pin the mixed-origin vector.

5. **BROKEN** — TypeScript resolution against a virtual installed package produced:

   - `node10`: root → `dist/src/core/index.d.cts`; server → `UNRESOLVED`.
   - `node16`, `bundler`, and `nodenext`: both subpaths resolved to the expected `.d.ts` or `.d.cts` declaration.

   ESM and CommonJS imports of both built barrels executed successfully. `dist/bin/main.js` passed `node --check`, has a Node shebang, imports an included server entry, and is mode `0644`.

   `npm pack --dry-run --json` could not produce the requested tarball reading because npm attempted a cache write and failed with `EROFS`; setting a temporary cache also failed in the read-only sandbox. Running npm’s installed packlist implementation instead returned `LICENSE`, `README.md`, `package.json`, the bin and map, and the core/server ESM, CommonJS, declarations, and maps. Thus the actual tar header and installed bin shim remain unobserved, but the universal claim is already falsified by Node10 server resolution. The manifest also carries a stale `sideEffects` entry for unshipped `./src/bin/main.ts`. Smallest correct fix: add a Node10-compatible declaration mapping for `server`—normally `types` plus `typesVersions`—and remove the unshipped side-effect row; then build and inspect the real tarball and installed bin.

6. **BROKEN** — The existing `test:guides` source proves structural parity in both directions: its set-equality assertion compares documented surface rows with exported declarations, and further assertions compare interface members, implementation prototypes, barrel coverage, and examples. The uncovered gap is behavioral truth.

   Concrete drift includes the guide’s contained-path claim despite symlink escape; its assertion that a same-shaped caller file survives everywhere despite the reserved boot-path deletion; its claim that every package failure is a `ProbeError`; and its statement that `errors.test.ts` keeps every failure path categorized. That test only strips comments and searches source text for `new Error(`. Executing `readWorkspaceManifest(process.cwd(), 'definitely-absent-package')` raised native `Error` with `isProbeError(error) === false`, which the sweep cannot see.

   Candidate source overlays are fresh, and existing tests exercise repeated TypeScript, lint, and runtime paths. No previous claim’s `Source.text` leak was found. Configuration state does persist: TypeStage returns an existing service before reparsing a caller-named project, while Vitest and Oxlint configurations are resident. A modified configuration can therefore produce a different answer from a newly constructed stage, and the guide does not admit that cache boundary. Smallest correct fix: correct the false prose, replace the lexical error sweep with real failure-path executions, and either reload configuration state or document and test its lifetime.

7. **BROKEN** — The case-insensitive candidate sweep used these token patterns over whole Markdown and TypeScript comments/TSDoc:

   - `\b(?:zero|one|two|three|four|five|six|seven|eight|nine|ten|eleven|twelve|thirteen|fourteen|fifteen|sixteen|seventeen|eighteen|nineteen|twenty|thirty|forty|fifty|sixty|seventy|eighty|ninety|hundred|thousand|dozen|pair|both|\d+)\b`
   - `\b(?:first|second|third|fourth|fifth|sixth|seventh|eighth|ninth|tenth|last|final|penultimate|\d+(?:st|nd|rd|th))\b`

   Candidate-bearing paths were `README.md`; `guides/probe.md`; `src/core/{helpers,shapers,types,validators}.ts`; `src/server/{Overlay,Probe,ProbeServer,helpers,types}.ts`; all stage implementation files; `tests/src/core/{errors,helpers}.test.ts`; `tests/src/server/{Probe,ProbeServer,helpers}.test.ts`; and `tests/src/server/stages/LintStage.test.ts`. `guides/README.md` was a clean control.

   Substantiated violations include “five listeners” and “three listeners,” “first claim,” “second call/process/signal,” “last line,” the numbered receipt-field inventory, and prose referring to fields by indices and final position. These sets can change or are list items identified by position. Limits such as 64 specifications, versions, dates, durations, and sizes were excluded as directed. Smallest correct fix: name events and fields semantically, express the receipt as a named grammar, and replace mutable inventories with references to their declaring source.

8. **BROKEN** — Barrel and call-site inspection found:

   - `Inspection` is an internal queue record. No public operation accepts or returns it, so an outside consumer has no package seam through which to use it.
   - `TypeStageInterface.candidates` and the matching getter are reached only by TypeStage tests; production coordination never reads them.
   - `createProbe` and `createProbeServer` are one-line constructor wrappers. The exported classes expose exactly their interfaces, so the factories add no boundary, invariant, composition, translation, lifecycle, or materially narrower contract.

   Other exported support types are carried by public signatures, and the helper, stage, overlay, server, validator, formatter, shape, and constant exports have implementation or documented consumer uses. Smallest correct fix: intern `Inspection`, remove the test-only `candidates` member, remove both factories, and update the bin, README, guide, and tests to construct the classes directly.

9. **BROKEN** — The failure space overlaps and is incomplete. Executing the missing-manifest path returned an unbranded native `Error`, so not every thrown failure is a `ProbeError`. A caller-selected test path that matches no Vitest project returns `origin: 'instrument'`, while another caller path mistake raises `ProbeError` with `code: 'invalid'`. A missing caller-selected TypeScript project is `code: 'workspace'`. A deadline value names only the stage and duration, so it does not distinguish slow claimant code from a stalled instrument. TypeStage also classifies fileless compiler diagnostics as `instrument`, even when the caller-selected project caused them.

   Consequently, callers cannot reliably distinguish claimant fault from instrument fault by value alone; some cases require interpreting the message or execution context. Smallest correct fix: define one ownership discriminant used by both thrown errors and findings, translate native dependency/filesystem/parser failures into the declared error contract, and classify invalid claim paths and projects consistently as claimant faults while retaining a separate structured category for the underlying condition.

VERDICT: FAIL — 9 broken, 0 unresolved, 0 not-evidenced, 0 findings outside the claims