Read the governing rules and verified every cited site in both trees. Held the **subjective design-fit lane**.

| # | Class | Package | file:line | Rules broken | The defect in one sentence | The fix in one sentence |
|---|-------|---------|-----------|--------------|-----------------------------|--------------------------|
| 1 | VIOLATION · PRE-EXISTING | `@orkestrel/process` | `src/server/helpers.ts:856`, `:1004`; `src/core/types.ts:219`, `:247`, `:281`, `:311` | `.claude/rules/names.md:199` ("Never introduce synonyms such as `cancel`, `reset`, or `run` for these meanings") against the table at `:197` (`execute` — "Run primary work to completion"); AGENTS.md § Design laws "One concept, one term"; § Self-describing helpers (`run` is a bare verb, not `{verb}{Noun}`) | The package's one-shot execution primitive is named `run`/`runSync` with `RunResult`/`RunInput`/`RunOptions`/`RunSyncOptions`, which is the exact meaning the fixed-vocabulary table reserves for `execute` and names `run` as the banned synonym for. | Rename to `execute`/`executeSync` and `Execute*` and update every consumer in the same change (`src/core/errors.ts` `createRunError`, `src/server/helpers.ts` `buildRunResult` and `detach`, `tests/`, `guides/process.md`), per AGENTS.md "No compatibility shims." |
| 2 | VIOLATION | `@orkestrel/probe` | `src/server/Probe.ts:138,207,272,281,286,294,407,418`; `ProbeServer.ts:96,105,116`; `helpers.ts:53,136,161,165,169,191`; `stages/RuntimeStage.ts:102,109,335,442`; `stages/LintStage.ts:73,76,278,280,283`; `stages/TypeStage.ts:99,102,149,151,216,234,244` | `.claude/rules/typescript.md:45` ("Programmer error or invalid argument \| Throw an `AppError`"), `:68` ("Error classes expose a machine-readable `code` and optional `context`"), `:69` ("Every public error class ships with a guard such as `isAppError`") | All 33 failure paths throw a plain `new Error(...)`, and the package defines no error class, no `code`, no `context`, and no guard, so a consumer catching from `Probe`, `ProbeServer`, or any stage cannot narrow or branch on the failure. | Add `src/core/errors.ts` defining `ProbeError` (with `code`, optional `context`, and a branded `isProbeError` guard) mirroring `@orkestrel/process/src/core/errors.ts:11-53`, add its code union to `src/core/types.ts` and `constants.ts`, and route every programmer-error and invalid-argument throw through it — do not reuse `ContractError` from `@orkestrel/contract`, whose codes are contract-shape failures and whose semantics do not match probe's domain. |
| 3 | VIOLATION | `@orkestrel/probe` | `src/server/types.ts:39` reached through `src/server/index.ts:1`; `tests/guides.test.ts:17`, `:150`, `:171-177` | `.claude/rules/documentation.md:31-32` ("Every public export is documented"); `.claude/rules/architecture.md:249-251` ("If a declaration should not be public, make it a true local or runtime-private detail … Never leave an intentional reusable export stranded outside the barrel"), `:254-255` (interning means "out of the barrel, and named in the package's parity `INTERNAL` list") | `OverlayInterface` is named in `INTERNAL` as interned while `export * from './types.js'` publishes it as a real type export of `@orkestrel/probe/server`, so an undocumented name ships and `tests/guides.test.ts:150` filters it out of the parity population on a false premise; the gate at `:171-177` cannot catch this because `Object.keys(server)` never sees a type-only export. | Remove `'OverlayInterface'` from `INTERNAL` (keep `'Overlay'`, whose class is genuinely unbarrelled), document `OverlayInterface` in the `## Surface` table of `guides/probe.md`, add the `@example` that `tests/guides.test.ts:179-193` then requires and correct the TSDoc at `src/server/types.ts:35-37` that claims none is owed, and change the interning gate to scan `src/server/types.ts` source instead of `Object.keys(server)` so it can see a type. |
| 4 | JUDGMENT | `@orkestrel/probe`, `@orkestrel/process` | probe `stages/TypeStage.ts:50-51,160-164`, `stages/LintStage.ts:51-52,88-93`, `stages/RuntimeStage.ts:79-80,169-174`, `Probe.ts:66-67,170-175`; process `ProcessManager.ts:47-48,164-168`, `Process.ts:76,79,203-207,342` | AGENTS.md § Design laws "Derive state. Compute facts from existing fields. Do not store a second flag or label that can drift." | Six classes store a boolean (`#destroyed`, `#destroying`, `#terminating`) beside a promise-barrier field (`#closing`, `#ending`, `#stopping`) that the boolean can be computed from, which reads as duplicated state. | Retain the flags and record the ordering reason once. Option (a), delete each boolean and read `promise !== undefined`: the boolean is deliberately assigned on the line *before* the promise (`TypeStage.ts:162-163`, `ProcessManager.ts:166-167`, `Process.ts:342` inside the synchronous prefix of `#kill()`), so deriving it narrows every guard's coverage past a re-entrant window that `ProcessManager.ts:114-121` documents as reachable. Option (b), retain and state that ordering in one comment at each pair. Recommend (b): the fields cannot drift because each is assigned once, in one block, and nowhere else. |
| 5 | VIOLATION | `@orkestrel/probe` | `tests/src/core/index.test.ts:1-40`; `tests/src/server/index.test.ts:1-58` | `.claude/rules/tests.md:43` ("Do not create test files solely for `constants.ts`, barrels, error definitions, or `types.ts`") | Both files exist solely for a barrel: every assertion is `Object.keys(entry)` membership, a definedness loop, or a `prototype` member list, with no call into the exported behavior. | Delete both files and move what they uniquely prove into `tests/guides.test.ts`, which already owns export parity — the interface-member checks at `tests/src/server/index.test.ts:41-57` belong beside the `documents exactly the members each behavioral interface declares` test at `tests/guides.test.ts:155-169`. |
| 6 | VIOLATION | `@orkestrel/process` | `tests/src/server/index.test.ts:1-53` | `.claude/rules/tests.md:43` (same line) | Both `it` blocks assert only `@src/server` barrel membership and `typeof` on the exported symbols, with no call into `Process`, `ProcessManager`, `run`, or `detach`. | Delete the file and leave export parity to `tests/guides.test.ts`, which already derives its population from real source scanning. |
| 7 | VIOLATION | `@orkestrel/probe` | `guides/probe.md:478` | `.claude/rules/writing.md:51-52` ("Point to other material with `preceding`, `following`, `earlier`, or `later`, never with `above` or `below`") | The sentence reads "three orders of magnitude inside the teardown **above**." | Replace `above` with `preceding`, naming the section it points at. |
| 8 | VIOLATION | `@orkestrel/process` | `guides/process.md:550-551` | `.claude/rules/writing.md:51-52` (same line) | The sentence reads "the bound **below** applies to a terminated run." | Replace `below` with `following`, naming the section it points at. |
| 9 | VIOLATION | `@orkestrel/process` | `guides/process.md:718` | `.claude/rules/writing.md` § Substitutions ("`simply`, `easy`, `just` → Delete") | The sentence reads "the child it **just** spawned is destroyed." | Recast to keep the temporal fact without the banned word: "the child spawned a moment earlier is destroyed." |

## Dropped

- Architecture #2 (`probe src/server/Overlay.ts`) — the lane's own verdict is "No action"; it evidences compliance, not a defect.
- Typescript #2 (`process src/core/errors.ts`) — the lane's own clean statement, no rule line cited as broken.
- Typescript #3 (bans on `any`/`as`/`!`/suppression comments, access modifiers, default exports, readonly, import extensions, nested functions) — the lane's own clean statement across both packages, no rule line cited as broken.
- Documentation #1's second half, that the TSDoc at `src/server/types.ts:35` is a false claim — the sentence's subject is `Overlay`, the class, and `src/server/index.ts` carries no row for `./Overlay.js`, so that sentence is true as written; the barrel-leak half of the row is retained as merged row 3.
- Documentation #1's attribution of the leak to `@orkestrel/process` — a self-corrected transcription error in the lane's own cell, no separate defect.
- Architecture lane's statement that `@orkestrel/process`'s barrel exposes `ProcessError` — false: `tests/src/server/index.test.ts:6-41` enumerates the server barrel and `ProcessError` is absent from it; it is a core export. No finding either way.
- Tests lane referral on file-local helpers in `probe/tests/src/bin/main.test.ts`, `Probe.test.ts`, `stages/LintStage.test.ts` — `.claude/rules/tests.md:159` names no observable trigger these helpers meet, and the lane found no cross-file duplication; a helper narrow to one file's fixture concept is not a breach.
- Tests lane referral on probe's missing `tests/setup*.test.ts` proof for `setupServer.ts` — the lane's own quoted rule at `.claude/rules/tests.md:21` states where such a proof resolves if one exists and does not require one.
- Names, patterns, and tests lanes' "checked clean" blocks — not findings.

**Referral to the Orchestrator (outside my lane):** row 1's rename is a breaking change to a published surface. The catalog at `/workspace/process/.claude/agents/orkestrel.md:55,69,70` pins `@orkestrel/process ^0.0.3` in `@orkestrel/mcp` 0.0.19, `@orkestrel/scaffold` 0.0.44, and `@orkestrel/sea` 0.0.9; I could not inspect those trees, so whether any of them calls `run`/`runSync`/`RunResult` — and therefore whether row 1 is a single-package fix or a fleet cascade the user must authorize — is unresolved here.

VIOLATION rows: `@orkestrel/probe` 4 (rows 2, 3, 5, 7); `@orkestrel/process` 4 (rows 1, 6, 8, 9).